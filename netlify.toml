[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Frontend deployment settings
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# API proxy to Railway backend (add your Railway URL when ready)
[[redirects]]
  from = "/api/*"
  to = "https://your-railway-app.railway.app/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/mystical-status"
  to = "https://your-railway-app.railway.app/mystical-status"
  status = 200
  force = true
