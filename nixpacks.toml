[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]  # This is the BUILDER phase - installs dependencies
cmds = ['npm install']

[phases.build]
cmds = ['echo "Build complete"']

[start]  # This is the DEPLOY phase - runs the app
cmd = 'npm start'
